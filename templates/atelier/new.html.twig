{% extends 'base.html.twig' %}

{% block title %}Nouvel Atelier{% endblock %}

{% block body %}
    <div class="container mt-5">
      <h1 class="mb-4 text-center">Créer un Atelier</h1>

      <div class="card">
        <div class="card-body">
          {{ include('atelier/_form.html.twig') }}
        </div>
      </div>

      <a href="{{ path('app_atelier_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}
