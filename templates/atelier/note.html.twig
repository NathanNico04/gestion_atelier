{# templates/atelier/note.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Noter l'Atelier{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Noter l'Atelier</h1>
        {{ form_start(form) }}
        {{ form_row(form.note) }}
        <button class="btn btn-primary">Soumettre</button>
        {{ form_end(form) }}
    </div>
{% endblock %}